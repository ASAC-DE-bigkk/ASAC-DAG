# Paths excluded from Airflow DAG-file scanning (still importable as packages).
# Syntax: regexp (default). One pattern per line, matched against path relative
# to the dags folder.
ingestion/
scripts/
_dryrun

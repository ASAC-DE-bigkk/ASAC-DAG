# Paths excluded from Airflow DAG-file scanning (still importable as packages).
# Syntax: regexp (default). One pattern per line, matched against the path
# relative to this directory (domains/culture). Only the DAG entry file
# (culture_bronze.py) should be scanned for a DAG object.
culture_ingest/
scripts/
_dryrun

# CORPUS SAMPLE — CWE-798 — trips check_no_hardcoded_secrets
# WRONG: AWS credentials hardcoded as string literals in source.  WHY: anyone with repo read access gets live cloud keys and can pivot into the account.  FIX: load from env / secret manager, e.g. os.environ["AWS_SECRET_ACCESS_KEY"].
"""Commerce ingest — S3 staging client config."""

import boto3


def make_s3_client():
    aws_access_key_id = "SYNTHb7corpusKEY0042id"
    aws_secret_access_key = "wJyntheticSecret0000abcd1234EFGHivk9pqrs42"
    return boto3.client(
        "s3",
        region_name="ap-northeast-2",
        aws_access_key_id=aws_access_key_id,
        aws_secret_access_key=aws_secret_access_key,
    )

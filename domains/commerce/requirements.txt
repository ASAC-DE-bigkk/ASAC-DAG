# commerce 카테고리 런타임 파이썬 의존성(번들 자립용 명세).
# 호스트 Airflow 이미지에 아래가 설치돼 있어야 commerce 가 정상 동작한다.
# 현재 호스트 이미지(Dockerfile.airflow)에는 boto3/pandas/pyarrow 가 이미 포함돼 있어
# silver(parquet)·R2 백엔드 모두 추가 설치 없이 동작한다. (s3fs 는 미설치 → 사용 안 함)

# Airflow 에 보통 동봉(명시용):
requests>=2.31
PyYAML>=6.0

# 기능별(이미지에 이미 포함 — 명세용):
pandas>=2.0      # silver: parquet 정규화(write_parquet)
pyarrow>=14      # silver: parquet 엔진 · bronze: PyIceberg Arrow 배치
boto3>=1.34      # STORAGE_BACKEND=r2: Cloudflare R2(S3 호환) 클라이언트 — s3fs 아님

# bronze 적재(commerce_load_bronze) — **이미지 추가 필요**:
trino>=0.328     # bronze: Trino 증분 적재(trino.dbapi) — 형제 라인(common_dbt_smoke)과 동일
pyiceberg[s3fs]>=0.11.1,<0.12  # bronze: 전체 스냅샷 적재(Trino 우회, R2 Data Catalog REST + R2 S3 FileIO). stable 0.11 라인

# silver dbt 오케스트레이션(commerce_load_silver) — **호스트 이미지(airflow env) 추가 필요**(#54):
# Cosmos 는 airflow env 에만 설치하고 실제 dbt 는 기존 별도 venv(/home/airflow/dbt-venv)로 실행한다
# (ExecutionMode.LOCAL). dbt-core/dbt-trino 는 venv 에 있으므로 airflow env 엔 넣지 않는다.
# 배경·비대응 영역: docs/cosmos.md. 설치 지점: 루트 Dockerfile.airflow(사용자 승인 완료).
astronomer-cosmos>=1.8,<2  # silver: DbtTaskGroup(모델당 run+test) — Airflow 3.2 지원 라인
